# Angular 2 with TypeScript and Webpack

## Setup

Install Dependencies `npm install`

Start Webpack Dev Server `npm start`

Start Webpack Build `npm run build`

Typescript Lint with Codelyzer `npm run lint`
